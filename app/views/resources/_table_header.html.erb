<thead>
  <tr>
	  <% column_names.each do |column| %>
	    <th class="cell <%= column %>">
				<%=  resource_class.human_attribute_name(column.to_sym).titleize %>
	    </th>
	  <% end %>
  </tr>
</thead>